# @package _global_
defaults:
  - /gateway: mixed
  - _self_

bot_name: CSP Bot

# Tokens are read from the environment: SLACK_BOT_TOKEN, SLACK_APP_TOKEN, DISCORD_TOKEN
# csp-bot-start --config-dir=example +bot=mixed
